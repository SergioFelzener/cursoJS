const nome = "rebeca";
const concatenacao = "olá " + nome + " !";
//criando template string abre com `crase 
const template = `
                    olá 
                    ${nome} !`;

console.log (concatenacao, template);

// expressoes
console.log(`1 + 1 = ${1 + 1}`);

// funcao atribuida a variavel..
const up = texto => texto.toUpperCase();
console.log (`Ei .... ${up(`cuidado mestre`)}!`);








